# Machines Index

## Resolution

- Prefer `~/.codex/skills/memory-skill/scripts/resolve-machine.sh` to derive
  the current host key.
- Normalize hostnames by stripping `.local`, lowercasing, and replacing
  non-alphanumeric runs with `-`.
- Map normalized hostnames to machine identifiers here.
